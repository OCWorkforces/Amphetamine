- [ ] The prompt is realistic, this is a natural prompt that a real developer would ask
      - [ ] The prompt does not add arbitrary constraints / additions
            - [ ] All constraints / additions are natural relative to the prompt
      - [ ] The prompt does not contain an endless list of constraints, details or specify how to do every little thing
      - [ ] The prompt requests something of value(no toy or inconsequential asks)
      - [ ] The prompt does not request something that would not be a realistic addition to the repository, e.g. adding a custom data pipeline directly onto the pandas repository
- [ ] The prompt should contain minimal details on how to implement it.
- [ ] The prompt has a concrete goal
      - [ ] The prompts final goal is unambiguous
      - [ ] The prompt does not ask the model to find something wrong with the codebase
- [ ] The repo is sufficiently challenging
      - [ ] The repo is not empty or a toy project
      - [ ] The repo has meaningful complexity, complex tasks can be derived from it
      - [ ] Opus either can’t solve the problem, or takes longer than 10min to solve it
- [ ] The rubrics make it clear what an ideal solution looks like
      - [ ] The rubrics clearly define what an ideal response looks like and are free of ambiguities such as requests for (clean modular code, detailed response)
      - [ ] They will be used in the future as a reward rubric during RL and will be used to create verifier methods for reward function and possibly also fed to GraderAgent as an eval.
            - [ ] So all rubrics that we write should be something that a verifier function or an agent could verify as well.
      - [ ] Let's take a rubric:  The feature XYZ is properly implemented . This is bad because the word proper is subjective and will vary model to model. So rubrics shouldn't have subjective assertions

Bad:

- [ ] `There should be enough test cases for the feature XYZ`  -- again subjective

Good:

- [ ] `The tests should check the edge cases EC1, EC2 & EC3` -- this is good.
      - [ ] On a refactoring task:  `The methods exported/exposed by the package/class shouldn't change.`
- [ ] The prompt was not generated by an LLM
      - [ ] The prompt is written in natural tone and is not overly explicit in asks, this prompt resembles my own personal prompts to LLM models
- [ ] The agent does not require network dependencies/docker/containerisation/local LLMs to complete the task
- [ ] The prompt does not work with basic CRUD/Demo applications
- [ ] I am fully confident in my ability to assess the results of the prompt
- [ ] I did not used an open source issue that already has a merged solution
